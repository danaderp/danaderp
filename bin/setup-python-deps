#!/usr/bin/env bash
set -euo pipefail

echo "Installing optional Python deps for jekyll-jupyter-notebook..."
if ! python3 -m pip install --user --break-system-packages jupyter nbconvert; then
  python3 -m pip install --user jupyter nbconvert
fi

echo
echo "If your shell cannot find jupyter binaries, add this to your shell profile:"
echo '  export PATH="$HOME/Library/Python/3.14/bin:$PATH"'
